# Create Your First Mobile App

This project documents the creation of a mobile application using
Expo Router, modification of the home screen, and project reset behavior.
## Project Reset

The project was reset using the following command:

npm run reset-project

This reset-project command clears cached files and resets the project
to a clean development state while keeping source files intact.
